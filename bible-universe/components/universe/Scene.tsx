'use client';
/**
 * Scene — React Three Fiber canvas + scene graph.
 *
 * Phase 2 will implement:
 *   - <Canvas> with PerspectiveCamera (fov=60)
 *   - OrbitControls with damping + distance limits
 *   - Ambient + directional lights
 *   - PointLight at Jesus origin (gold, intensity 4)
 *   - <Starfield /> — 15,000 particle background
 *   - <EraDiscs /> — semi-transparent cluster floor discs
 *   - <Nodes /> — all BibleNode spheres
 *   - <Connections /> — beam lines between nodes
 *   - Post-processing: UnrealBloom + Vignette (via @react-three/postprocessing)
 */

export default function Scene() {
  return null;
}
