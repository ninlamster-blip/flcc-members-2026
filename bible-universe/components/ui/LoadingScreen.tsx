'use client';
/**
 * LoadingScreen — full-screen cover shown while the 3D scene initializes.
 *
 * Fades out once store.isLoadingComplete = true.
 * Uses Framer Motion AnimatePresence for smooth exit.
 *
 * Visual:
 *   - Deep cosmos background
 *   - "Bible Universe" title with gold glow
 *   - Animated progress bar
 *   - Subtitle: "From Creation to Revelation"
 */

export default function LoadingScreen() {
  return null;
}
