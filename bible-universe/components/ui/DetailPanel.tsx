'use client';
/**
 * DetailPanel — slides in from the right when a node is selected.
 *
 * Sections:
 *   - Type badge  (color-coded by node type)
 *   - Node label  (large serif title)
 *   - Era + period
 *   - Description paragraph
 *   - Key scriptures (verse chips)
 *   - Connected nodes list  (click → flyTo + selectNode)
 *   - Prophecy fulfillment  (only for prophecy type nodes)
 *
 * Phase 5 will implement the full panel.
 * Uses Framer Motion AnimatePresence for slide-in/out.
 */

export default function DetailPanel() {
  return null;
}
