'use client';
/**
 * PaulsJourneyLayer — animated 3D route visualization.
 *
 * Phase 8 will implement:
 *   - 4 animated route lines (TubeGeometry along waypoints)
 *   - Each route has a distinct color (cyan / yellow / orange / red)
 *   - Animated dot traveling the route (useFrame lerp along CatmullRomCurve3)
 *   - City markers at each waypoint (small sphere + label)
 *   - Timeline slider: scrub through the journey chronologically
 *   - Scripture reference popup at each waypoint
 *   - Toggle to show all 4 journeys simultaneously or one at a time
 *
 * Note: This layer lives in 3D space alongside the main knowledge graph.
 * It overlays the Church Age era cluster (z ≈ -700).
 */

export default function PaulsJourneyLayer() {
  return null;
}
