'use client';
/**
 * ProphecyLayer — OT → NT fulfillment arc network.
 *
 * Phase 9 will implement:
 *   - High-arcing orange Bézier curves from OT prophecy nodes → NT fulfillment nodes
 *   - Each arc has an animated particle flowing from prophecy to fulfillment
 *   - The arcs all converge on / diverge from the Jesus supernode at the origin
 *   - Toggle view: "show OT prophecies only" / "show NT fulfillments only" / "show both"
 *   - Click an arc to open a split detail panel showing the prophecy + its fulfillment
 *     side by side with scripture text
 *
 * Key prophecy arcs (from Isaiah 53, Micah 5:2, Psalm 22, Genesis 3:15,
 * Daniel 9, Jeremiah 31, Zechariah 9:9, etc.)
 */

export default function ProphecyLayer() {
  return null;
}
