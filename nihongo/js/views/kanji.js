// Placeholder for the "kanji" view — implemented in a later phase.
export function render(root) {
  root.innerHTML = `
    <div class="empty-state fade-in">
      <div class="big">🚧</div>
      <h2 style="font-weight:800;font-size:20px;color:var(--text);">Coming in the next phase</h2>
      <p>This module is being built incrementally.</p>
    </div>`;
}
